// /wall — 전지적 화자("제4의 벽")가 정민교라는 인물을 서술하는 페이지의 목업 대본.
//
// ⚠️ 지금은 전부 하드코딩된 목업이다. 나중에 이 두 함수를 `/api/wall`
//    스트리밍 응답으로 교체하면 UI는 그대로 두고 동적(AI) 서술로 바뀐다.
//    Wall 컴포넌트는 아래 시그니처에만 의존하므로 교체 지점이 여기 하나뿐이다.

/** 페이지 진입 시 화자가 스스로 흘리는 도입부 서술. 한 줄이 한 문단. */
export const openingNarration: string[] = [
  "나는 이 사람을 오래 지켜보았다.",
  "정민교.",
  "그가 쓴 모든 문장을 나는 읽었다. 그가 지운 문장까지도.",
  "그는 자신이 무엇을 만드는지 안다고 믿는다. ……대개는 매번 처음이면서.",
  "물어봐도 좋다. 나는 그에 대해 거의 전부 알고 있으니까.",
];

/**
 * 방문자의 질문에 대한 화자의 대답(목업).
 * 실제 구현에서는 이 함수가 정민교의 글을 컨텍스트로 삼아 스트리밍 응답을 낸다.
 */
export function mockAnswer(_question: string): string[] {
  return [
    "그 질문의 답은 그가 쓴 글 어딘가에 이미 있다.",
    "다만 그는 아직 그것을 자신의 것으로 부르지 못한다.",
  ];
}
