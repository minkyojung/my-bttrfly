# 이 파일에 적힌 커밋은 git blame에서 건너뛴다. 내용은 그대로 두고 표기만 바꾼
# 커밋이라, blame에 잡히면 원래 그 문장을 쓴 커밋을 가린다.
#
# 로컬에도 적용하려면 한 번 설정해 둔다:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub은 이 파일을 자동으로 존중한다.

# style(content): normalize markdown to the editor's output
bc72eb126e68140002d250a1917d9e2fe63eac5f
